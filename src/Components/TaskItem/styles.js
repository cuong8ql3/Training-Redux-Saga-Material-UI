const styles = theme => ({
  card: {
    marginTop: 10,
    backgroundColor: theme.color.cardColor,
  },
  cardActions: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
})
export default styles
